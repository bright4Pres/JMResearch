rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{uid} {
      // Allow user to create their own profile document after authenticating
      allow create: if request.auth != null && request.auth.uid == uid;

      // Allow read for owner; staff (custom claim) can list/read all
      allow read: if request.auth != null && (request.auth.uid == uid || request.auth.token.role == 'staff');
      allow list: if request.auth != null && request.auth.token.role == 'staff';

      // Allow updates by owner but prevent client writes to 'role'
      allow update: if request.auth != null
                    && request.auth.uid == uid
                    && !( 'role' in request.resource.data );

      // Only admin (staff) can delete
      allow delete: if request.auth != null && request.auth.token.role == 'staff';
    }

    // Add other rules as needed
  }
}
